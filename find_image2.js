const https = require('https'); https.get('https://en.wikipedia.org/wiki/Will_Dyson', { headers: { 'User-Agent': 'Antigravity/1.0' } }, (res) => { let data = ''; res.on('data', chunk => data += chunk); res.on('end', () => { const matches = data.match(/src=.[^>]+(jpg|png)/gi); console.log(matches ? matches.filter(m => m.toLowerCase().includes('cannon') || m.toLowerCase().includes('dyson')) : 'not found'); }); });
