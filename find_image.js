const https = require('https'); https.get('https://en.wikipedia.org/wiki/Treaty_of_Versailles', { headers: { 'User-Agent': 'Antigravity/1.0' } }, (res) => { let data = ''; res.on('data', chunk => data += chunk); res.on('end', () => { const matches = data.match(/src=.([^.]+)/g); console.log(matches ? matches.filter(m => m.toLowerCase().includes('dyson') || m.toLowerCase().includes('cannon') || m.toLowerCase().includes('weep')) : 'not found'); }); });
