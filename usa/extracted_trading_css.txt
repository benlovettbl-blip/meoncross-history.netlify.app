<style id="trading-cards-style">
  .scumbag-binder-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 20px;
  }
  .scumbag-card-container {
    background-color: transparent;
    perspective: 1000px;
    height: 350px;
    border-radius: var(--border-radius-md);
    cursor: pointer;
    position: relative;
  }
  .scumbag-flip-card-inner {
    position: relative;
    width: 100%;
    height: 100%;
    text-align: center;
    transition: transform 0.6s;
    transform-style: preserve-3d;
  }
  .scumbag-card-container.flipped .scumbag-flip-card-inner {
    transform: rotateY(180deg);
  }
  .scumbag-flip-card-front, .scumbag-flip-card-back {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    -webkit-backface-visibility: hidden;
    backface-visibility: hidden;
    border-radius: var(--border-radius-md);
    overflow: hidden;
    box-shadow: var(--shadow-sm);
    border: 1px solid var(--border-glass);
  }
  .scumbag-flip-card-back {
    transform: rotateY(180deg);
    background: var(--surface-light);
    display: flex;
    flex-direction: column;
    padding: 15px;
    overflow-y: auto;
  }
  .scumbag-card-locked .foil-pack-body {
    border-radius: var(--border-radius-md);
  }
</style>